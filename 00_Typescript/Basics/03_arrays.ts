// Normal Array
let data: (string | number)[] = ["Virat", 18, "Rohit", 45, "Gill", 99];

let player: [string, number] = ["Virat", 18];

//let players = ["Alice", "Bob", "Charlie"];

//console.log(players[0]); // Output: Alice
//console.log(players[1]); // Output: Bob

// Loop through the Array
for (const player of players) {
    console.log(player);
}